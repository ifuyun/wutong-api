import CheckPostIdInterceptor from './check-post-id.interceptor';

describe('CheckPostIdInterceptor', () => {
  it('should be defined', () => {
    expect(new CheckPostIdInterceptor()).toBeDefined();
  });
});
